import { DebouceKeyupDirective } from './debouce-keyup.directive';

describe('DebouceKeyupDirective', () => {
  it('should create an instance', () => {
    const directive = new DebouceKeyupDirective();
    expect(directive).toBeTruthy();
  });
});
